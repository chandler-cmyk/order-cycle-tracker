{
  "name": "order-cycle-tracker",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.1",
    "node-fetch": "^3.3.2"
  },
  "scripts": {
    "dashboard:server": "node dashboard-server.js",
    "dashboard:client": "cd dashboard-client && npm start",
    "dashboard:build": "cd dashboard-client && npm install && npm run build",
    "inventory": "node inventory-server.js"
  },
  "engines": {
    "node": "20.x"
  }
}
